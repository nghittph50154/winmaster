<Window x:Class="WinMaster.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:WinMaster.ViewModels"
        xmlns:views="clr-namespace:WinMaster.Views"
        mc:Ignorable="d"
        Title="WinMaster"
        Width="1280" Height="800"
        MinWidth="1000" MinHeight="640"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundDeepBrush}"
        WindowStyle="None"
        AllowsTransparency="True"
        ResizeMode="CanResize"
        d:DataContext="{d:DesignInstance vm:MainViewModel}">

    <Window.Resources>
        <!-- DataTemplates for navigation -->
        <DataTemplate DataType="{x:Type vm:InstallViewModel}">
            <views:InstallView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:PlaceholderViewModel}">
            <views:PlaceholderView/>
        </DataTemplate>

        <!-- Fade-in animation for content panels -->
        <Storyboard x:Key="FadeIn">
            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.2"/>
        </Storyboard>
    </Window.Resources>

    <!-- Custom window chrome -->
    <Border Background="{StaticResource BackgroundDeepBrush}"
            CornerRadius="12"
            BorderThickness="1"
            BorderBrush="{StaticResource BorderSubtleBrush}">
        <Border.Effect>
            <DropShadowEffect Color="#000000" BlurRadius="40" ShadowDepth="8" Opacity="0.6"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="52"/>   <!-- Title bar -->
                <RowDefinition Height="64"/>   <!-- Nav bar -->
                <RowDefinition Height="*"/>    <!-- Content -->
            </Grid.RowDefinitions>

            <!-- ═══ TITLE BAR ═════════════════════════════════════════════ -->
            <Border Grid.Row="0"
                    Background="{StaticResource BackgroundBrush}"
                    CornerRadius="12,12,0,0"
                    MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid Margin="20,0">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <!-- Logo icon -->
                        <Border Width="28" Height="28" CornerRadius="7" Margin="0,0,10,0">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="#6C63FF" Offset="0"/>
                                    <GradientStop Color="#00D4AA" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                            <TextBlock Text="W" FontSize="16" FontWeight="Bold"
                                       Foreground="White"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"/>
                        </Border>
                        <TextBlock Text="WinMaster"
                                   FontSize="15" FontWeight="SemiBold"
                                   Foreground="{StaticResource TextPrimaryBrush}"
                                   VerticalAlignment="Center"
                                   Margin="0,0,8,0"/>
                        <Border Background="{StaticResource SurfaceElevatedBrush}"
                                CornerRadius="4" Padding="6,2"
                                VerticalAlignment="Center">
                            <TextBlock Text="{Binding AppVersion}"
                                       FontSize="11"
                                       Foreground="{StaticResource TextMutedBrush}"/>
                        </Border>
                    </StackPanel>

                    <!-- Window controls -->
                    <StackPanel Orientation="Horizontal"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Center">
                        <Button Width="28" Height="28"
                                Content="–"
                                FontSize="16"
                                Foreground="{StaticResource TextSecondaryBrush}"
                                Background="Transparent"
                                BorderThickness="0"
                                Cursor="Hand"
                                Click="MinimizeButton_Click"
                                ToolTip="Minimize">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border x:Name="b" Background="Transparent" CornerRadius="6">
                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="b" Property="Background" Value="{StaticResource SurfaceHighBrush}"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </Button.Style>
                        </Button>
                        <Button Width="28" Height="28"
                                Content="□"
                                FontSize="13"
                                Foreground="{StaticResource TextSecondaryBrush}"
                                Background="Transparent"
                                BorderThickness="0"
                                Cursor="Hand"
                                Click="MaximizeButton_Click"
                                ToolTip="Maximize">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border x:Name="b" Background="Transparent" CornerRadius="6">
                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="b" Property="Background" Value="{StaticResource SurfaceHighBrush}"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </Button.Style>
                        </Button>
                        <Button Width="28" Height="28"
                                Content="✕"
                                FontSize="13"
                                Foreground="{StaticResource TextSecondaryBrush}"
                                Background="Transparent"
                                BorderThickness="0"
                                Cursor="Hand"
                                Click="CloseButton_Click"
                                ToolTip="Close">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border x:Name="b" Background="Transparent" CornerRadius="6">
                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="b" Property="Background" Value="#CC2222"/>
                                                        <Setter Property="Foreground" Value="White"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </Button.Style>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- ═══ NAVIGATION BAR ════════════════════════════════════════ -->
            <Border Grid.Row="1"
                    Background="{StaticResource BackgroundBrush}"
                    BorderBrush="{StaticResource BorderSubtleBrush}"
                    BorderThickness="0,0,0,1">
                <StackPanel Orientation="Horizontal"
                            VerticalAlignment="Center"
                            Margin="16,0">

                    <!-- INSTALL tab -->
                    <Button Style="{StaticResource NavButtonStyle}"
                            Command="{Binding NavigateToCommand}"
                            CommandParameter="install">
                        <Button.Content>
                            <TextBlock Text="INSTALL" FontSize="13" FontWeight="SemiBold"/>
                        </Button.Content>
                        <Button.Foreground>
                            <SolidColorBrush Color="{Binding IsInstallActive, Converter={StaticResource ActiveMenuToBrush}, ConverterParameter=color}"/>
                        </Button.Foreground>
                    </Button>

                    <!-- Nav indicator for Install -->
                    <Border Width="2" Height="20" Background="{StaticResource BorderSubtleBrush}" Margin="4,0"/>

                    <!-- MENU 2 tab -->
                    <Button Style="{StaticResource NavButtonStyle}"
                            Command="{Binding NavigateToCommand}"
                            CommandParameter="menu2">
                        <Button.Content>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="⚙" FontSize="14"/>
                                <TextBlock Text="TWEAKS" FontSize="13" FontWeight="SemiBold"/>
                                <Border Background="{StaticResource SurfaceElevatedBrush}"
                                        CornerRadius="4" Padding="5,1">
                                    <TextBlock Text="SOON" FontSize="9" Foreground="{StaticResource TextMutedBrush}"
                                               FontWeight="SemiBold"/>
                                </Border>
                            </StackPanel>
                        </Button.Content>
                    </Button>

                    <!-- MENU 3 tab -->
                    <Button Style="{StaticResource NavButtonStyle}"
                            Command="{Binding NavigateToCommand}"
                            CommandParameter="menu3">
                        <Button.Content>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="🔧" FontSize="14"/>
                                <TextBlock Text="SYSTEM TOOLS" FontSize="13" FontWeight="SemiBold"/>
                                <Border Background="{StaticResource SurfaceElevatedBrush}"
                                        CornerRadius="4" Padding="5,1">
                                    <TextBlock Text="SOON" FontSize="9" Foreground="{StaticResource TextMutedBrush}"
                                               FontWeight="SemiBold"/>
                                </Border>
                            </StackPanel>
                        </Button.Content>
                    </Button>

                    <!-- Active indicator bar -->
                    <StackPanel.Resources>
                        <Style TargetType="Button" BasedOn="{StaticResource NavButtonStyle}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsInstallActive}" Value="True">
                                    <!-- Handled in code-behind via UpdateNavIndicator -->
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </StackPanel.Resources>
                </StackPanel>
            </Border>

            <!-- Active nav indicator (bottom border of active tab) -->
            <Border Grid.Row="1" VerticalAlignment="Bottom" Height="2" Margin="16,0,0,0">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                        <GradientStop Color="#6C63FF" Offset="0"/>
                        <GradientStop Color="#00D4AA" Offset="0.5"/>
                        <GradientStop Color="Transparent" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
            </Border>

            <!-- ═══ CONTENT AREA ══════════════════════════════════════════ -->
            <ContentControl Grid.Row="2" Content="{Binding CurrentView}"/>
        </Grid>
    </Border>
</Window>

